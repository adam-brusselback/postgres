# Settled results — the index

Branch-local; delete with this directory.

Every measured number this project relies on, with **the protocol that produced
it** and **where the derivation lives**. This file owns no derivations. It
exists because two facts have already been re-derived — once wrongly — after
being recorded in one file and looked for in another.

**The rule this file enforces: a figure may not be quoted without its
protocol.** A number without its protocol is not a result, it is a rumour. Two
of the mistakes below were exactly that:

- `B16` sat "OPEN, unconfirmed" in ISSUES.md while PLAN.md 3.1 had closed it
  with a direct measurement. Answered twice, by different routes.
- `+28.6%` was retracted as unreproducible. PLAN.md 4.1 groups by **span**; the
  recheck grouped by **scope rows** — a different partition, since span 1 on
  `nonkey` is scope 100 — got different numbers, and read them as a
  contradiction. The figure was correct.

**Status values.** `settled` — measured, protocol recorded, reproduced.
`provisional` — measured once, or on a protocol since found biased.
`unresolvable` — the effect is smaller than the harness's own noise floor, and
no number should be quoted at all. `superseded` — kept because it is still
cited elsewhere and someone will otherwise re-derive it.

---

## Performance

| # | question | answer | protocol | derivation | status |
|---|---|---|---|---|---|
| R1 | generic vs custom plan | **21.7% slower** net; **+28.6%** on range/span 1 (best +72.8%), **−33.5%** on array/span 100 | 94 comparisons, 7 workloads × 3 shapes × 3 spans × 2 paths, 6 warm-ups; **grouped by span, not scope rows** | PLAN.md 4.1; `bench/optmatrix.sql`; `opt_result` | settled |
| R27 | **the 12.20 µs split: rewrite or plan?** | **plan, overwhelmingly.** `pg_plan_query` alone **41.25 µs**, `AcquireRewriteLocks` + `QueryRewrite` **1.13 µs** — planning is **97.3%** of the combined line, the rewrite 2.7%. So 3.7's ceiling is the plan half and the rewrite is not worth designing around | `profile.py` with `srcplan` split out of `srcrewrite`, `-O2` **no assertions**, in-backend, 300 × `REFRESH ... WHERE id = 1` on `projection` scope 1, `querytree=on optimized=off`; 9 warm bands of 30, band 1 discarded (it carries the cold prepare) | `scratch/r27.sh`; CACHE.md §1 | settled |
| R28 | **3.7 is necessary but NOT sufficient** | `srcplan` is **41.25 µs of a 74.16 µs deficit — 56%**. `srcbuild` (5.30) and `srcexec` (9.94) are most of the rest, and the latter is work the text arm does inside `dmlexec`, so it is not a like-for-like saving. **R26's explanation — "the whole deficit is one line" — is false on this measurement** | both arms in one run, same clone, same boot: text 75.42 µs against Query-tree 149.58 µs per refresh | `scratch/r27b.sh`; CACHE.md §1 | settled |
| R29 | **the plan cache is not being dropped by background activity** | **`prepare = 0.0` in all 9 warm bands of both arms** — 270 refreshes each, plans prepared once in band 1 and never rebuilt. Direct confirmation of PLAN.md 3.1, and the observation that removed B2's restructure from 3.7's critical path (CACHE.md §4) | as R27 | CACHE.md §4; ISSUES.md B2 | settled |
| R2 | where the time goes | source planning **16.2% → 0.9% → 0.0%** at scope 1/100/10k; fused DML **52% → 58% → 87%**; pre-lock **17.8% → 10.3%** | `INSTR_TIME` timers around every phase, `-O2`, 300 scope-1 refreshes, instrumentation reverted after | PLAN.md "Tier 1 and 2 sized" and "The locking SELECT, researched" | settled |
| R3 | row comparison, zero churn | **54–58% faster** on `nonkey`/scope 10000 | **settled heap**, VACUUM before each timed refresh, arms alternated, 15 samples | SPECIALIZE.md §1; `bench/churn.sql` | settled |
| R4 | row comparison vs churn | **54–56 → 41–51 → 27–36 → 18–27 → ≈0** at churn 0/5/25/50/100; break-even **60–70%** | as R3; ranges are two protocols disagreeing | SPECIALIZE.md §1; `bench/churn.sql` | settled |
| R5 | heap state | the same comparison reads **54% or 82%** on identical code, data and boot; **54% is the honest one** | 10 clones, novac bands vs VACUUM-before-each | SPECIALIZE.md §1; `bench/heapstate.sh` | settled |
| R6 | mutability | `no_delete` **13–19% faster** at scope ≥1000 (1.1% on `expensive`); `append_only` **+10–22 points** on top, **26–39%** total | fresh-clone model, **on top of** the row comparison, not instead of it | SPECIALIZE.md §1; `bench/mutability.sql` | settled |
| R7 | commit amortisation (D1 vs D2) | **3.5–3.7×** at scope 1, **1.25–1.9×** at scope 100, then **inverts** to 3.3× *slower* at scope 10000 | `bench/run.sh --perxact`, 20 refreshes per transaction vs 1 | ISSUES.md B16; SPECIALIZE.md §1 | settled |
| R8 | concurrency | **0 deadlocks, 0 failed txns, 0 serialization failures** over 54 cells; disjoint **3.4–5.9×** at 4 clients; `nonkey` range/scope 1000 hot **161 → 141 → 26 tps** at 1/4/16 | `bench/run.sh`, single boot verified from the server log | SPECIALIZE.md §1 and §5 | settled |
| R9 | match/merge crossover | **none.** Direct modification **1.31–1.63×** (`aggregate`) and **1.68–2.09×** (`timerange`) from 10% to 90% scope | measured before the routing change, so `bare`/`conc` stand in for the two algorithms | SPECIALIZE.md §5 | settled |
| R10 | pre-lock `ORDER BY` | **20–69% of the pre-lock** when misaligned, growing with scope; **4–26%** when aligned. The pre-lock is 12–15% of a refresh (R2), so ~3–10% of a refresh is **implied, not measured** | `bench/orderby.sh`, plain-heap clone, row comparison on so neither arm writes | SPECIALIZE.md §1 and §3 | settled |
| R11 | `ORDER BY`, whole-refresh | **do not quote a number.** Effect (1–7%) is below the floor | see R12 | `bench/orderby-floor.sh` | **unresolvable** |
| R12 | the noise floor | baseline wobbles **6.8–11.3%** between clones; **1 measurement in 40** lands **73% above** the median; no plan flips; position within a clone is inside the wobble | 10 clones, all forms timed on **each** clone | SPECIALIZE.md §6; `bench/orderby-floor.sh` | settled |
| R13 | source plan caching | **12.2 µs faster** per refresh, **16.2%** at scope 1, ~0 at scale.  **This is the canonical share — not 19.8% or 22%.**  It is the cost of the thing to be removed, measured as one line covering *rewrite and plan together*; the split is R27 and the saving cannot exceed the plan half | as R2, and a fixed cost, so it only shows at scope 1 | SPECIALIZE.md §3; PLAN.md 3.7; CACHE.md §1 | settled |
| R14 | deparse elision | **4.5–5.3 µs faster**, **7.4%** at scope 1 | as R13 | SPECIALIZE.md §3; PLAN.md 3.8 | settled |
| R15 | `Const` parameterisation | **8× faster** in-backend, by turning plan-cache misses into hits.  **End to end it is 2.0x** and the two do not conflict -- see R38, which reconciles them.  Implemented; measured across shape and span as **R37** | PLAN.md 3.2 | SPECIALIZE.md §3; R37; R38 | settled |
| R16 | `ORDER BY` on the pre-lock, aligned vs not | ~~3.6×~~ | superseded by R10, which measures 1.2–1.7× | PLAN.md "The locking SELECT" | **superseded** |
| R17 | `FOR UPDATE` cost | the cost is the **locking, not the scan** — it defeats the index-only scan, 5.4 ms of 6.5 ms at scope 10000 | clean 100,000-row table | PLAN.md "The locking SELECT" | settled |
| R26 | **Query-tree path against text path, warm, CONSTANT LITERAL predicate** | **24% slower** at scope 1 (49.7 µs text against 61.6 µs Query-tree); confirmed at a different absolute level over three alternating pairs, 60.1 against 78.4, which is **30%**.  The whole deficit is one line: 12.20 µs re-planning the source query.  On a *cold* cache the Query-tree path **wins** (233 against 315 µs), because it skips `pg_get_viewdef` and re-parsing the view text | as R2 — `INSTR_TIME` timers, `-O2`, **`REFRESH ... WHERE id = 1` x300** on `projection`, whose span-1 predicate is `id = :k`.  **The predicate mode was unrecorded and three reviewers each had to infer it; it is a constant literal, so `params` is NULL and plancache serves a generic plan unconditionally** | PLAN.md "Tier 1 and 2 sized" and 3.1; CACHE.md §1 | **provisional — the absolutes did not reproduce.** Re-run on the same protocol (R27/R28) gives text **75.42** against Query-tree **149.58** µs, a **98%** deficit, not 24%.  Both arms slowed, the Query-tree arm by more (2.43× against 1.52×).  Cause unestablished: the container is not the one R2 ran on and the tree has changed.  **The direction and the sign hold; do not quote the magnitude until it is re-measured** |
| R35 | **Query-tree path against text path, END TO END, the 2.1b sweep** | **about +6% for the Query-tree path, and flat across predicate shape**: array **+5.6%** (23 cells, -2.2 to 13.3), key **+6.9%** (8 cells, 0.5 to 15.5), range **+5.4%** (23 cells, -7.0 to 17.4).  **The per-workload column is noise-limited at `--repeat 3` and must not be quoted or ranked** -- `timerange` read +0.0% over 5 cells and re-measures **+7.8%** at `--repeat 5` (`tr-probe`, same build and boot), with both of its negative cells flipping positive and the same spi cell moving 3.344 -> 2.705 ms.  `recursive` +0.5% is the one per-workload figure that holds, because it is arithmetic rather than measurement: ~120 us against a ~1400 ms refresh is 0.009%.  The shape averages stand on 23/8/23 cells and are independently corroborated by Jul-30 `p21-O2` on a different build.  The absolute saving is roughly **fixed at 120-157 us for scope <= 1000** (bands 1-10 / 100 / 1000 give 120 / 150 / 157, `recursive` excluded -- see protocol), then **jumps to 2963 us at scope 10000+**, which a fixed cost does not explain.  `recursive` corroborates the fixed reading from the other end: at ~1400 ms a refresh, 120 us is 0.009% and it measures ~0 (6 of its 7 cells mildly negative, -0.4 to -2.2) | `bench/run.sh --shapes key,array,range --forms spi,querytree --label p21c-O2`, run in five chunks because the container is reclaimed on inactivity and two earlier attempts at this sweep were lost whole.  `-O2`, **assertions off**, `matview_partial_refresh_querytree=on` **verified from the running server**, scale 100000, groups 1000, clients 1, disjoint, mutate off, sync off, perxact 1, 8 workloads, 108 rows, shape census 16:46:46 — **not** the 16:48:48 a full sweep gives, because `timerange` span 100 is skipped for both array and range by the **KEYMAX guard**, not by `--maxscope`: its key column is `bucket` with `KEYMAX = :scale/1000 = 100`, so span 100 "covers all 100 keys, which is a full refresh wearing a predicate, not a partial one" (`run.sh:160`).  An earlier revision of this row credited `--maxscope 90`; that is a different check with a different meaning and it did not fire.  **Absolute savings must not be pooled across workloads**: `recursive` refreshes take ~1400 ms against 0.8-65 ms elsewhere, so averaging us over all eight gives -4813 us for one band, which is an artefact of mixing scales, not a measurement | `bench/sweep-p21c-O2.md`; PLAN.md 2.1b | settled |
| R36 | **where a refresh actually spends its time, by profile** | **At scope 1 the source query is 40-47% of the whole refresh, and the plan cache misses on every one.**  Text 46.8% / Query-tree 39.7%, and the entire 7.1-point gap is the deparse (6.6% -> 0.0%); planning is *unchanged* (19.2 vs 19.8) and `SPI_prepare` is 14.6 vs 12.8, i.e. both arms re-plan every refresh.  The cause is **already recorded in PLAN.md 3.2 and was rediscovered here, not discovered**: pgbench interpolates `:k` client-side, the cache key is the deparsed predicate text, so a varying literal misses every time.  3.2 states it outright -- *"every measurement in every run recorded so far is on the miss path"* -- and sizes it in-backend over 300 refreshes at **546 us varying / 72 us constant / 67 us bound parameter, i.e. 8x**.  **Quote 3.2's numbers, not this row's.**  A quick end-to-end check here gave only **1.81x** (0.728 ms varying against 0.403 ms constant) and does not reconcile with 8x; it was a single 12-second pgbench pair with no alternation and no settle -- the exact protocol `profile.sh` exists to replace -- and part of it is data locality besides, since a constant k re-refreshes one hot row.  The discrepancy is unexplained and 3.2 has the better protocol.  **At scope 10000 the source query is 0.04% in BOTH arms** -- four parts in ten thousand -- so nothing the rewrite touches can move a large-scope refresh.  There the cost is `ON CONFLICT` 23-27%, locking SELECT 19-20%, base scan 18-26%, buffers 12%, sort 7-9%, WAL 6% | `bench/profile.sh window range 100 4 20` and `... projection key 1 4 20`: one fixture built once for both arms, arms **alternated** with the order flipped each round, `settle()` between every capture, and each capture **validated** to be >=50% under `refresh_by_direct_modification` or discarded.  perf 6.8.12 at 299 Hz, `--call-graph dwarf`, on the **same -O2 assertions-off binary that produced R35** -- gcc emits .eh_frame without `-g`, so no profiling build was needed and no build-variance caveat applies.  8 captures per scope, all validated (80-91% at scope 10000, 51-57% at scope 1, the lower figure being the commit and client round trip a 0.7 ms refresh cannot avoid) | `bench/profile.sh`; `bench/sweep-p21c-O2.md`; **PLAN.md 3.2** | settled, except the 1.81x figure which is **superseded by PLAN.md 3.2's 8x** |

| R37 | **parameterising the predicate's constants, END TO END** | **40 paired cells, all 40 faster**, by **0.3% to 45.3%**, saving **40 to 1053 us**.  The saving is a **fixed cost** -- a plan built once instead of on every call -- so the percentage reports how cheap the refresh is, not how much work was removed: `expensive`/key/1 is 45.3% of a 0.78 ms refresh and `nonkey`/array/100 is 0.3% of a 54 ms one, and the microseconds are 352 against 179.  Per shape, averaged over cells: key **+25.5%** (6 cells), array **+18.3%** (17), range **+9.9%** (17).  **range is the weak shape and the reason is on record**: PLAN.md 4.1 measured that `auto` mis-handles a narrow range predicate -- the generic estimate assumes a wide range, so the custom plan always wins on estimated cost and the statement re-plans on every execution -- so range keeps the parse-and-analyse saving and loses the planning one.  **No cell degrades as the scope grows**: at scope 10000 on `nonkey` the refresh is ~50 ms and the saving is still positive.  The param arm carries a ~42 us handicap the implementation does not (see R38), so every figure here is a lower bound | `bench/run.sh --forms conc --shapes key,array,range --spans 1,10,100 --predmode literal|param --repeat 4`, six workloads, arms paired **per workload** so drift cannot be read as an effect; `-O2`, assertions off, one build, one boot.  **`recursive` and `window` are not in it, deliberately**: `recursive` refreshes in ~1400 ms against a fixed ~250 us effect, which is 0.018% and unresolvable, and pooling its milliseconds with everyone else's microseconds is what produced the bogus band in R35; `window` produced no cells in R35 either, its key space tripping the KEYMAX guard on every shape.  **`timerange`'s five cells are from a re-probe at `--repeat 8 --time 12`**, after the sweep read four of them negative -- see below | `bench/sweep-predmode.md`; PLAN.md 3.2 | settled |
| R38 | **what the `--predmode` axis measures, and what it cannot** | **The wrapper costs 42 us and parameterisation is worth 248 us**, key shape, scope 1, `projection`: lit **415.6**, wrap **457.9**, param **210.3**, const **155.0** us per refresh.  REFRESH takes no parameters directly, so binding one means `EXECUTE ... USING` in a DO block, and `run.sh`'s param arm pays that while its literal arm does not -- so the axis measures (parameterisation - wrapper) and every R37 figure is a **lower bound**.  `const` is the hit floor but re-refreshes one hot row, so read it as a floor and not as the target.  **This also reconciles R15's 8x with R36's unexplained 1.81x**: 8x is an in-backend ratio on a 67 us base, and end to end the same effect is **2.0x** because the client round trip and one commit per refresh (~83 us here) sit in both arms' denominators.  R36's discrepancy is closed | `bench/predparam.sh`, 8 rounds x 300 refreshes, four arms with the order rotated each round, round 1 discarded, session setup measured and subtracted, **no VACUUM between timed blocks** (X11), `synchronous_commit=off` -- with it on, every refresh carries a ~1 ms WAL flush and the same effect reads 1.26x | `bench/predparam.sh`; RESULTS.md R15, R36 | settled |

## Correctness instruments

| # | question | answer | protocol | derivation | status |
|---|---|---|---|---|---|
| R18 | differential harness | **re-run and current: 23 shapes, 0 errors, and every shape's divergence count identical to the recorded pristine vector.**  The one new row is `nullable_key_nd`, defined after the baseline was taken, reading 0/0; `calibrate.baseline` is now 46 rows and the staleness this entry carried is closed.  The older figure was 21 shapes / 1533 mutations / 0 divergence.  Loud under B4 (**76/96** and **12/48**) | two matviews, one base, refreshed each way and diffed | PLAN.md 1.1; `safety/rundiff.sh` | settled |
| R19 | concurrent fuzzer | **6 of 7 caught, 1 missed for a known reason, quiet on pristine.** M1 `p2`+`nest` 72/100 deadlocks · M2 `p3` 25/50 · M3 `serial` 32 lost updates · M6 `serial` 27 · **C2 `nest` 50/100 wrong plan** · **A4 all four modes 100/100 failed refreshes** · **B4 MISSED** — the fixture is `id int PRIMARY KEY`, and B4 duplicates NULL-keyed rows, so the mutation has nothing to act on.  A fixture limitation, not a hole; the NULL shapes live in the differential corpus, whose job that is | `calibrate-fuzz.sh`, `ITER=25 REFRESHERS=4`, each mutation applied to `matview.c`, rebuilt, and the whole fuzzer run against it.  An earlier calibration at `ITER=40` gave M1 76/160, M2 40/80, M3 45, M6 55 — comparable in verdict, not in count | PLAN.md 1.1b; `fuzz.sh` | settled |
| R30 | the concurrent fuzzer's `nest` mode | **calibrated: catches C2 in 5 runs of 5**, at **49–50 of 100** — essentially every refresh in the `querytree=on` arm.  C2 removes B14's guard, so a nested refresh shares the session cache and the use-after-free is live | `fuzz.sh nest`, both GUC arms alternated across sessions, an `ALTER TABLE` invalidator committing in a separate backend, 4 sessions × 25 | `fuzz.sh`; ISSUES.md B14 | settled |
| R31 | do `p2`/`p3` notice a refresh that never ran? | **they did not.** With A4 applied — `ON CONFLICT` dropped, so every refresh dies of a unique violation — **40 of 40 failed**, the transactions rolled back, the matview was therefore still correct, and the run reported "no deadlocks", "matview matches its definition" and **PASS**.  Both modes now count non-deadlock errors and fail on them | A4 applied, `fuzz.sh p2 p3`, 4 sessions × 10 | `fuzz.sh` | settled |
| R32 | **every mutation against every instrument, both directions** | **17 CAUGHT, 3 benign-and-quiet, 0 UNCAUGHT, pristine QUIET**, over a corpus of 20.  Caught: A4 B4 B7 C1 C2 L1 M1 M2 M3 M6 O1 O2 P1 Q1 Q2 Q3 S1.  Correctly quiet: B4b M4 **L2** -- the last is a control, 63f516f undone, and any instrument firing on it would be asserting the mechanism rather than the guarantee.  **Two rounds, and the second is the one to quote.**  Round 1 found B7 and Q2 uncaught (ISSUES.md B27/B28) and then, with the corpus extended, **O1 and O2** -- the row comparison never emitted, and the flag selecting it dropped from the plan-cache key -- uncaught by all four instruments (B30/B31); `matview_where` Test 18 and `matview_where_cache` Test 6 close them and were seen to fail first.  Round 2 re-ran everything after `run_specs()` was fixed: it had called only the injection-points module, so the four isolation specs were in no matrix this harness had ever printed.  That moved **M1 quiet -> FIRED (matview-where-lockorder)** and **M2 quiet -> FIRED (matview-where-insertorder)**, both of which had been credited to the probabilistic fuzzer alone, and raised A4 2->6, L1 1->3, M3 and M6 1->2 | `calibrate-all.sh`, one build per mutation, four instruments each: regression suite, differential oracle, concurrent fuzzer, and specs -- the last being the injection-points module **and** the four isolation specs, which is what round 2 fixed.  20 specs run per mutation, up from 16; the liveness threshold moved with it so a runner that dies reads NORUN rather than quiet.  `-O2 --enable-cassert --enable-injection-points` | `calibrate-all.sh`; ISSUES.md B26-B28, B30-B31 | settled |
| R33 | **the noise floor of the 3.7 protocol** | **sd 4.11 us on a 67.40 us cell -- 6.1%**, over 72 bands; min 59.63, max 79.33, so a single band sits up to **29.2%** from the extremes.  Standard error of the mean over 72 bands is **0.48 us**, so a few-us effect is resolvable *as a mean* while no per-band number means anything -- one band can be 19.7 us out, larger than R13's entire 12.20 us line.  Between-run drift of the *same* arm was 4.0 us (72.08 against 68.08), inside the sd, which is why every deficit below is read within one run and never across two.  **R12 does not apply here** and quoting it would have given an acceptance band wide enough to hide the whole effect: it is a between-clone wobble on a 2.5 ms cell, this is an in-backend `INSTR_TIME` cell fifty times smaller | `bench/srcplan.sh floor 8`; `-O2`, **assertions off**, injection points off -- R27/R28's build.  `projection`, scale 100000, scope 1, **constant literal**, 300 *committed* refreshes per session, 8 sessions, band 1 of each discarded (it carries the cold prepare), **no VACUUM between timed refreshes** | CACHE.md §6; `bench/srcplan.sh` | settled |
| R34 | **does caching the source plan close the Query-tree deficit?** | **Yes, essentially entirely -- 54.25 us to 0.38 us, a 99.3% reduction.**  Cache off: text **72.08**, Query-tree **126.33**, deficit **54.25 us**.  Cache on: text **68.08**, Query-tree **68.46**, deficit **0.38 us** -- inside R33's floor and indistinguishable from zero.  The source line goes from **44.88 us** (srcbuild 3.60 + srcrewrite 7.29 + srcplan 33.99) to **0.07 us**.  **This contradicts R28's "necessary but not sufficient"**, and the reason is attributable rather than mysterious: R28 credited 3.7 with `srcplan` alone, 41.25 of a 74.16 us deficit, 56%.  As implemented it also removes `srcbuild` and `srcrewrite`, because the source *query* is only built on a cache miss -- a deliberate choice at CACHE.md step B, worth ~11 us more than the subplan anticipated.  The share closed is the whole source line, not the plan half | as R33, `arms 6` -- 6 rounds alternating off/on, 54 bands per arm.  The "before" arm is the same tree, build and container with `mutations.py C4` applied, which switches the source-plan cache off and nothing else, so the comparison sits inside one configuration rather than across two runs | CACHE.md §6 and §8; `bench/srcplan.sh`; PLAN.md 3.7 | settled |
| R40 | **the parameterisation measured against the code it replaces, not against a proxy** | **A varying literal goes 415.6 → 184.0 µs, a 231.6 µs saving, and now performs like a constant one.**  The ratio that matters is `lit / const` — a varying literal against the same predicate held constant, which is the hit floor — and it goes **2.68× before, 1.13× after**, with **3.31× under `mutations.py C5`** on the same build.  The remaining 13% is data locality: `const` re-refreshes one already-warm row.  Two consequences worth recording.  **The `--predmode param` axis is now meaningless on a patched build** — `param` reads 234.9 against `lit` 184.0, *slower*, because the `EXECUTE`-in-a-`DO`-block wrapper is pure overhead once the server parameterises for you; the axis only means anything on the unpatched code, which is why the sweep (R37) had to be run before this landed and cannot be repeated after.  **And the constant-literal caller does not regress**: 155.0 µs unpatched against 163.0 patched, with **identical minima of 144 and 146 µs** — the least noise-contaminated statistic of the two runs.  The concern was real in mechanism (before the change `params == NULL`, so `choose_custom_plan()` short-circuits and the plan is generic unconditionally and forever; after it, plancache runs its usual comparison) but the cost is five plan builds once, amortised to nothing, and the plan it settles on is the same plan | `bench/predparam.sh`, 8 rounds × 300 refreshes, four arms with the order rotated per round, round 1 discarded, `-O2` assertions off, `synchronous_commit=off`, no VACUUM between timed blocks.  **Read the ratios, not the absolutes**: the C5 arm was taken ~15 minutes after the patched arm on a container that had drifted (its `lit` reads 641.1 against the unpatched run's 415.6 for the same configuration), so every cross-build absolute here carries that.  The `lit / const` ratio is a within-run statement and does not | `bench/predparam.sh`; RESULTS.md R37, R38 | settled |
| R41 | **does parameterising cost plan quality on a SKEWED column?** | **No, and the control is what says so.**  Every workload in `workloads.sql` has uniformly distributed keys, so every cell of every sweep measures the case where a generic plan and a value-specific one are the same plan.  On a column where one key holds 10,000 rows and another holds 10: a fresh session refreshing the rare key reads **229.0 µs**, and a session that refreshes the *common* key twelve times first reads **249.0 µs** — a ratio of **1.09×**, which looks like the hazard.  Under **`mutations.py C5`, where the two predicates cannot share a plan at all, the same ratio is 1.17×** — higher.  So the ratio is the harness, not the change: the common-key refresh reads 10,000 rows and evicts the rare key's pages.  The mechanism that makes this safe is `PARAM_FLAG_CONST`, which lets `eval_const_expressions()` fold the value back in, so a custom plan is exactly the plan the literal got, and `choose_custom_plan()` stays on custom when the generic estimate is bad | `bench/predskew.sh`, 6 rounds × 100 refreshes, arms alternated, round 1 discarded, row comparison **on** so a common-key refresh reads 10,000 rows and writes none.  **Timing is psql's own `\timing` summed over the rare-key statements only.**  The first version wall-clocked the arm and subtracted a separately measured 374 ms warm-up block against 28 ms of measurement; a 10% swing in the subtrahend landed as ~370 µs per refresh, larger than the whole effect, and it reported **1.40×** from an arm whose minimum was *below* the other arm's.  Both figures above are from the repaired protocol | `bench/predskew.sh`; PLAN.md 3.2 | settled |
| R39 | **is the predicate parameterisation gated, and are its gates gates?** | **Yes, and they catch different bugs, which is why there are two.**  `mutations.py C5` turns the parameterisation off — what the code did before, and *still correct*: `matview_where_source_plan` part 3 goes red with **exactly five added notices**, one per varying-literal refresh, while part 1 stays silent because with `params == NULL` plancache serves a generic plan unconditionally.  Test 19 stays green under C5, correctly — there is nothing wrong to see.  **`C6`** leaves it on and breaks the **binding** (`lcons` for `lappend`, so the Consts land in the reverse of their paramid order): `matview_where` goes red **three ways** — Test 19's swapped constants leave `(2,1)` stale while `(1,2)` is refreshed twice; its text case raises `operator does not exist: text = integer`; and Test 18's `BETWEEN 1 AND 5` becomes `BETWEEN 5 AND 1` and selects nothing — and `matview_where_cache` catches it too.  A single-constant predicate is unaffected by C6 by construction, which is why the detector had to be a case carrying two | one build per arm, `-O2 --enable-cassert --enable-injection-points`, flipping only `matview.c`; full regress plus the injection-points module each way.  Pristine after both: **regress 5/5, injection_points 5 regress + 12 specs, oracle 23 shapes / 0 errors matching the recorded vector, `fuzz.sh` PASS on all four modes** | `mutations.py` C5/C6; `matview_where` Tests 18–19; `matview_where_source_plan` | settled |
| R20 | mutation corpus | **20/20 apply** against the current tree.  O1, O2, L1 and L2 added for the row comparison, its cache key and the pre-lock's strength (L2 a control, expected to be invisible); C2 added earlier (reinstates B14's use-after-free); **B6 deleted** — its target, the primary-key preference, was dead code and is gone (ISSUES.md B26) | `mutations.py --check`; an edit declares its expected occurrence count | ISSUES.md B23 | settled |
| R21 | what the oracle cannot see | P1 admits **no deterministic test**: a correct implementation has no window to exploit, so there is nowhere to put an injection point | — | PLAN.md 1.1b and 1.4 | settled |
| R22 | the B14 use-after-free, and that its test is a test | **unfixed 249/250, fixed 250/250.** Test 5 fails on unfixed with `ERROR: cannot change materialized view "mv_c4_inner"` from a refresh of `mv_c4`; standalone reproducer shows `0x7F` fill in the error context | one `-O2 --enable-cassert` build, flipping only `matview.c` between arms, full `installcheck` each way | ISSUES.md B14 | settled |
| R23 | why the first version of that test passed | `REFRESH` runs under `RestrictSearchPath()`, so **unqualified names in a nested function do not resolve**; the `ALTER TABLE` raised, the `EXCEPTION` block swallowed it, the nested refresh never ran | observed directly by replacing the trap with `RAISE NOTICE` | ISSUES.md B14 | settled |
| R24 | same-matview nesting | **blocked**, by `CheckTableNotInUse()`: *"cannot REFRESH MATERIALIZED VIEW … because it is being used by active queries in this session"* | run, not read: `scratch/uaf/selfnest.sql` | ISSUES.md B14; RESULTS.md X8 | settled |
| R25 | the suite under `debug_discard_caches = 1` | **5/5 green**, first time it has been run. `matview_where` **85 s against 409 ms**, which is the evidence the setting was in effect rather than silently ignored | `pg_regress --dbname=regression_dcc` with `PGOPTIONS='-c debug_discard_caches=1'`, assert build | SPECIALIZE.md §7b | settled |

## Retracted, and why — read before re-deriving

| # | claim | what happened |
|---|---|---|
| X1 | row comparison worth 72–78% | fresh-heap protocol. `bench_setup` hands the writing arm a heap with no free space and nothing dirtied since the last checkpoint, and only that arm writes. Worth 14–25 points. See R3, R5 |
| X2 | mutability is "the largest structural saving available" | reasoned from the prune sitting inside the phase that is 87% of a refresh (R2). A **share is not a saving**. Measured, it is smaller than the row comparison. See R6 |
| X3 | D1 is "D2 minus the commit cost" | inverts above scope 100. See R7 |
| X4 | "drop both `ORDER BY`s" is one decision | one saving and one regression, and the second is unresolvable. See R10, R11 |
| X5 | `+28.6%` does not reproduce | it does — the recheck regrouped it. See the header of this file, and R1 |
| X6 | a batch-scoped cache needs no invalidation because locks are held | false. `LockRelationOid()` calls `AcceptInvalidationMessages()`, so your own `table_open()` can invalidate what you cached. See SPECIALIZE.md §7a |
| X7 | B14 is fixed | reopened once. "Frees at the next refresh" — and a nested refresh is the next refresh. Fixed again and now covered by R22; the retraction stays because the first fix was believed on a green suite that could not have caught it |
| X8 | the mismatch branch is a second use-after-free site, needing no invalidation | it is not. It frees only under the OID its own caller passed, and same-matview nesting is blocked before any cache code (R24). ISSUES.md already contradicted itself about this in two places. It is cache thrash, not memory-unsafety |
| X10 | 3.7 may not pay, because plancache will pick a custom plan and re-plan anyway | wrong cell.  R26 was measured with a **constant literal**, where `params == NULL` and `choose_custom_plan()` returns generic unconditionally.  The custom-plan risk is real only for a **bound parameter**, which is a different cell — and one already dominated by the fused DML's own re-planning (PLAN.md 4.1, 138.7 against 51.4 µs).  Three independent reviews caught this; see CACHE.md §1 and §9 |
| X11 | VACUUM before each timed refresh is the honest heap protocol, everywhere | only where one arm writes and the other does not (X1).  `vac_update_relstats()` updates `pg_class` in place, which implies a relcache invalidation, so on a plan-cache measurement it converts a **warm** cell into a **cold** one — and the Query-tree path already wins cold.  See CACHE.md §6 |
| X12 | the `nest` mode cannot gate B14's class — 0 catches in 9 runs against a broken build | **wrong, and self-inflicted.** The wrong-plan counter was anchored `^ERROR:`; `psql -f` prefixes messages with `psql:FILE:LINE: `, so the pattern could not match the output that mode produces. The detector had been working the whole time. Corrected calibration is R30. Fourth instrument in this directory to fail silently — a detector reporting zero is indistinguishable from one that cannot report |
| X9 | a green suite means the fix is verified | the first B14 fix was believed this way, and so was the first version of its test (R23). **A test that has not been seen to fail is not evidence** — record both arms or record nothing |

---

## Provenance hazards that have produced wrong answers here

Recorded once, in SPECIALIZE.md §6. Summarised only: cross-boot comparison;
fresh-heap bias; per-arm sample counts; integer division in a reporting query;
measuring under the noise floor. **Check R1–R21 before measuring anything** —
twice now, it had already been measured.
